<def f='halide/llvm-install/include/llvm/ADT/APInt.h' l='1714' ll='1718' type='unsigned int llvm::APInt::countTrailingOnes() const'/>
<doc f='halide/llvm-install/include/llvm/ADT/APInt.h' l='1706'>/// Count the number of trailing one bits.
  ///
  /// This function is an APInt version of the countTrailingOnes
  /// functions in MathExtras.h. It counts the number of ones from the least
  /// significant bit to the first zero bit.
  ///
  /// \returns BitWidth if the value is all ones, otherwise returns the number
  /// of ones from the least significant bit to the first zero bit.</doc>
