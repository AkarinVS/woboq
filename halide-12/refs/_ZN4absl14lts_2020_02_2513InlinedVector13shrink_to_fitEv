<def f='halide/build-apps/abseil-cpp/absl/container/inlined_vector.h' l='737' ll='741' type='void absl::lts_2020_02_25::InlinedVector::shrink_to_fit()'/>
<doc f='halide/build-apps/abseil-cpp/absl/container/inlined_vector.h' l='726'>// `InlinedVector::shrink_to_fit()`
  //
  // Reduces memory usage by freeing unused memory. After being called, calls to
  // `capacity()` will be equal to `max(N, size())`.
  //
  // If `size() &lt;= N` and the inlined vector contains allocated memory, the
  // elements will all be moved to the inlined space and the allocated memory
  // will be deallocated.
  //
  // If `size() &gt; N` and `size() &lt; capacity()`, the elements will be moved to a
  // smaller allocation.</doc>
