<dec f='halide/llvm-install/include/llvm/ADT/APInt.h' l='296' type='void llvm::APInt::APInt(unsigned int numBits, ArrayRef&lt;uint64_t&gt; bigVal)'/>
<doc f='halide/llvm-install/include/llvm/ADT/APInt.h' l='289'>/// Construct an APInt of numBits width, initialized as bigVal[].
  ///
  /// Note that bigVal.size() can be smaller or larger than the corresponding
  /// bit width but any extraneous bits will be dropped.
  ///
  /// \param numBits the bit width of the constructed APInt
  /// \param bigVal a sequence of words to form the initial value of the APInt</doc>
