<def f='halide/llvm-install/include/llvm/Support/TargetRegistry.h' l='891' ll='894' type='static void llvm::TargetRegistry::RegisterMCRelocationInfo(llvm::Target &amp; T, Target::MCRelocationInfoCtorTy Fn)'/>
<doc f='halide/llvm-install/include/llvm/Support/TargetRegistry.h' l='882'>/// RegisterMCRelocationInfo - Register an MCRelocationInfo
  /// implementation for the given target.
  ///
  /// Clients are responsible for ensuring that registration doesn&apos;t occur
  /// while another thread is attempting to access the registry. Typically
  /// this is done by initializing all targets at program startup.
  ///
  /// @param T - The target being registered.
  /// @param Fn - A function to construct an MCRelocationInfo for the target.</doc>
