<def f='halide/build-apps/_deps/tflite-src/tensorflow/lite/interpreter.h' l='577' ll='579' type='void tflite::Interpreter::SetAllowBufferHandleOutput(bool allow_buffer_handle_output)'/>
<doc f='halide/build-apps/_deps/tflite-src/tensorflow/lite/interpreter.h' l='569'>/// Set if buffer handle output is allowed.
  //
  /// When using hardware delegation, Interpreter will make the data of output
  /// tensors available in `tensor-&gt;data` by default. If the application can
  /// consume the buffer handle directly (e.g. reading output from OpenGL
  /// texture), it can set this flag to false, so Interpreter won&apos;t copy the
  /// data from buffer handle to CPU memory. WARNING: This is an experimental
  /// API and subject to change.</doc>
