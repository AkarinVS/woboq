<dec f='halide/llvm-install/include/llvm/IR/ConstantRange.h' l='124' type='static llvm::ConstantRange llvm::ConstantRange::makeExactICmpRegion(CmpInst::Predicate Pred, const llvm::APInt &amp; Other)'/>
<doc f='halide/llvm-install/include/llvm/IR/ConstantRange.h' l='117'>/// Produce the exact range such that all values in the returned range satisfy
  /// the given predicate with any value contained within Other. Formally, this
  /// returns the exact answer when the superset of &apos;union over all y in Other
  /// is exactly same as the subset of intersection over all y in Other.
  /// { x : icmp op x y is true}&apos;.
  ///
  /// Example: Pred = ult and Other = i8 3 returns [0, 3)</doc>
