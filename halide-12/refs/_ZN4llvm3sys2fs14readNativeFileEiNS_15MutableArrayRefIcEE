<dec f='halide/llvm-install/include/llvm/Support/FileSystem.h' l='981' type='Expected&lt;size_t&gt; llvm::sys::fs::readNativeFile(llvm::sys::fs::file_t FileHandle, MutableArrayRef&lt;char&gt; Buf)'/>
<doc f='halide/llvm-install/include/llvm/Support/FileSystem.h' l='974'>/// Reads \p Buf.size() bytes from \p FileHandle into \p Buf. Returns the number
/// of bytes actually read. On Unix, this is equivalent to `return ::read(FD,
/// Buf.data(), Buf.size())`, with error reporting. Returns 0 when reaching EOF.
///
/// @param FileHandle File to read from.
/// @param Buf Buffer to read into.
/// @returns The number of bytes read, or error.</doc>
