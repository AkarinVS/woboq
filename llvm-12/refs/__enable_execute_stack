<def f='llvm/compiler-rt/lib/builtins/enable_execute_stack.c' l='41' ll='67' type='void __enable_execute_stack(void * addr)'/>
<doc f='llvm/compiler-rt/lib/builtins/enable_execute_stack.c' l='35'>// The compiler generates calls to __enable_execute_stack() when creating
// trampoline functions on the stack for use with nested functions.
// It is expected to mark the page(s) containing the address
// and the next 48 bytes as executable.  Since the stack is normally rw-
// that means changing the protection on those page(s) to rwx.</doc>
