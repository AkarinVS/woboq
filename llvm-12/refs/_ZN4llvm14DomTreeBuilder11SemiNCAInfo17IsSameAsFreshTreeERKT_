<def f='llvm/llvm/include/llvm/Support/GenericDomTreeConstruction.h' l='1542' ll='1558' type='static bool llvm::DomTreeBuilder::SemiNCAInfo::IsSameAsFreshTree(const DomTreeT &amp; DT)'/>
<doc f='llvm/llvm/include/llvm/Support/GenericDomTreeConstruction.h' l='1535'>// Check if the given tree is the same as a freshly computed one for the same
  // Parent.
  // Running time: O(N^2), but faster in practice (same as tree construction).
  //
  // Note that this does not check if that the tree construction algorithm is
  // correct and should be only used for fast (but possibly unsound)
  // verification.</doc>
