<def f='llvm/compiler-rt/lib/sanitizer_common/sanitizer_deadlock_detector.h' l='288' ll='293' type='bool __sanitizer::DeadlockDetector::onTryLock(DeadlockDetectorTLS&lt;BV&gt; * dtls, __sanitizer::uptr cur_node, __sanitizer::u32 stk = 0)'/>
<doc f='llvm/compiler-rt/lib/sanitizer_common/sanitizer_deadlock_detector.h' l='283'>// Handles the try_lock event, returns false.
  // When a try_lock event happens (i.e. a try_lock call succeeds) we need
  // to add this lock to the currently held locks, but we should not try to
  // change the lock graph or to detect a cycle.  We may want to investigate
  // whether a more aggressive strategy is possible for try_lock.</doc>
