<dec f='llvm/lldb/source/Plugins/StructuredData/DarwinLog/StructuredDataDarwinLog.h' l='45' type='static bool lldb_private::StructuredDataDarwinLog::IsEnabled()'/>
<doc f='llvm/lldb/source/Plugins/StructuredData/DarwinLog/StructuredDataDarwinLog.h' l='35'>/// Return whether the DarwinLog functionality is enabled.
  ///
  /// The DarwinLog functionality is enabled if the user explicitly enabled
  /// it with the enable command, or if the user has the setting set
  /// that controls if we always enable it for newly created/attached
  /// processes.
  ///
  /// \return
  ///      True if DarwinLog support is/will be enabled for existing or
  ///      newly launched/attached processes.</doc>
