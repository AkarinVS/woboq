<dec f='llvm/llvm/include/llvm/Demangle/ItaniumDemangle.h' l='5337' type='const size_t'/>
<doc f='llvm/llvm/include/llvm/Demangle/ItaniumDemangle.h' l='5332'>// `-0x1.ffffffffffffffffffffffffffffp+16383` + &apos;L&apos; + &apos;\0&apos; == 42 bytes.
    // 28 &apos;f&apos;s * 4 bits == 112 bits, which is the number of mantissa bits.
    // Negatives are one character longer than positives.
    // `0x1.` and `p` are constant, and exponents `+16383` and `-16382` are the
    // same length. 1 sign bit, 112 mantissa bits, and 15 exponent bits == 128.</doc>
