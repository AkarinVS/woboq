<dec f='llvm/lldb/include/lldb/Target/Target.h' l='1389' type='std::recursive_mutex'/>
<offset>5312</offset>
<doc f='llvm/lldb/include/lldb/Target/Target.h' l='1383'>/// classes make the SB interface thread safe
  /// When the private state thread calls SB API&apos;s - usually because it is
  /// running OS plugin or Python ThreadPlan code - it should not block on the
  /// API mutex that is held by the code that kicked off the sequence of events
  /// that led us to run the code.  We hand out this mutex instead when we
  /// detect that code is running on the private state thread.</doc>
<use f='llvm/lldb/source/Target/Target.cpp' l='4365' u='a' c='_ZN12lldb_private6Target11GetAPIMutexEv'/>
