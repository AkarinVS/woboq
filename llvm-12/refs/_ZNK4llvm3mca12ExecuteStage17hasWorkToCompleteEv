<inh f='llvm/llvm/include/llvm/MCA/Stages/Stage.h' l='45' c='_ZNK4llvm3mca5Stage17hasWorkToCompleteEv'/>
<def f='llvm/llvm/include/llvm/MCA/Stages/ExecuteStage.h' l='61' type='bool llvm::mca::ExecuteStage::hasWorkToComplete() const'/>
<doc f='llvm/llvm/include/llvm/MCA/Stages/ExecuteStage.h' l='55'>// This stage works under the assumption that the Pipeline will eventually
  // execute a retire stage. We don&apos;t need to check if pipelines and/or
  // schedulers have instructions to process, because those instructions are
  // also tracked by the retire control unit. That means,
  // RetireControlUnit::hasWorkToComplete() is responsible for checking if there
  // are still instructions in-flight in the out-of-order backend.</doc>
