<dec f='llvm/compiler-rt/lib/tsan/rtl/tsan_sync.h' l='118' type='void __tsan::MetaMap::ResetRange(__tsan::Processor * proc, __sanitizer::uptr p, __sanitizer::uptr sz)'/>
<use f='llvm/compiler-rt/lib/tsan/rtl/tsan_rtl.cpp' l='248' u='c' c='__tsan::UnmapShadow'/>
<def f='llvm/compiler-rt/lib/tsan/rtl/tsan_sync.cpp' l='121' ll='180' type='void __tsan::MetaMap::ResetRange(__tsan::Processor * proc, __sanitizer::uptr p, __sanitizer::uptr sz)'/>
<doc f='llvm/compiler-rt/lib/tsan/rtl/tsan_sync.cpp' l='115'>// ResetRange removes all meta objects from the range.
// It is called for large mmap-ed regions. The function is best-effort wrt
// freeing of meta objects, because we don&apos;t want to page in the whole range
// which can be huge. The function probes pages one-by-one until it finds a page
// without meta objects, at this point it stops freeing meta objects. Because
// thread stacks grow top-down, we do the same starting from end as well.</doc>
