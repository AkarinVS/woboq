<def f='llvm/compiler-rt/lib/scudo/standalone/combined.h' l='236' ll='240' type='void scudo::Allocator::commitBack(TSD&lt;scudo::Allocator::ThisT&gt; * TSD)'/>
<doc f='llvm/compiler-rt/lib/scudo/standalone/combined.h' l='231'>// Release the resources used by a TSD, which involves:
  // - draining the local quarantine cache to the global quarantine;
  // - releasing the cached pointers back to the Primary;
  // - unlinking the local stats from the global ones (destroying the cache does
  //   the last two items).</doc>
