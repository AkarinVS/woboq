<def f='llvm/build/include/c++/v1/atomic' l='621' ll='628'/>
<doc f='llvm/build/include/c++/v1/atomic' l='618'>// Figure out what the underlying type for `memory_order` would be if it were
// declared as an unscoped enum (accounting for -fshort-enums). Use this result
// to pin the underlying type in C++20.</doc>
<def f='llvm/libcxx/include/atomic' l='621' ll='628'/>
<doc f='llvm/libcxx/include/atomic' l='618'>// Figure out what the underlying type for `memory_order` would be if it were
// declared as an unscoped enum (accounting for -fshort-enums). Use this result
// to pin the underlying type in C++20.</doc>
