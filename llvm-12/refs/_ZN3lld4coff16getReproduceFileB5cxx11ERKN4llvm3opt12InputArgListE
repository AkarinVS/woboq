<def f='llvm/lld/COFF/Driver.cpp' l='1182' ll='1198' type='Optional&lt;std::string&gt; lld::coff::getReproduceFile(const opt::InputArgList &amp; args)'/>
<use f='llvm/lld/COFF/Driver.cpp' l='1275' u='c' c='_ZN3lld4coff12LinkerDriver10linkerMainEN4llvm8ArrayRefIPKcEE'/>
<doc f='llvm/lld/COFF/Driver.cpp' l='1175'>// lld has a feature to create a tar file containing all input files as well as
// all command line options, so that other people can run lld again with exactly
// the same inputs. This feature is accessible via /linkrepro and /reproduce.
//
// /linkrepro and /reproduce are very similar, but /linkrepro takes a directory
// name while /reproduce takes a full path. We have /linkrepro for compatibility
// with Microsoft link.exe.</doc>
