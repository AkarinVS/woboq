<dec f='llvm/llvm/include/llvm/Transforms/Utils/LoopUtils.h' l='82' type='bool llvm::formLCSSAForInstructions(SmallVectorImpl&lt;llvm::Instruction *&gt; &amp; Worklist, const llvm::DominatorTree &amp; DT, const llvm::LoopInfo &amp; LI, llvm::ScalarEvolution * SE, llvm::IRBuilderBase &amp; Builder, SmallVectorImpl&lt;llvm::PHINode *&gt; * PHIsToRemove = nullptr)'/>
<doc f='llvm/llvm/include/llvm/Transforms/Utils/LoopUtils.h' l='67'>/// Ensures LCSSA form for every instruction from the Worklist in the scope of
/// innermost containing loop.
///
/// For the given instruction which have uses outside of the loop, an LCSSA PHI
/// node is inserted and the uses outside the loop are rewritten to use this
/// node.
///
/// LoopInfo and DominatorTree are required and, since the routine makes no
/// changes to CFG, preserved.
///
/// Returns true if any modifications are made.
///
/// This function may introduce unused PHI nodes. If \p PHIsToRemove is not
/// nullptr, those are added to it (before removing, the caller has to check if
/// they still do not have any uses). Otherwise the PHIs are directly removed.</doc>
<use f='llvm/llvm/lib/Transforms/Scalar/LoopInterchange.cpp' l='1622' u='c' c='_ZN12_GLOBAL__N_124LoopInterchangeTransform18adjustLoopBranchesEv'/>
<def f='llvm/llvm/lib/Transforms/Utils/LCSSA.cpp' l='79' ll='292' type='bool llvm::formLCSSAForInstructions(SmallVectorImpl&lt;llvm::Instruction *&gt; &amp; Worklist, const llvm::DominatorTree &amp; DT, const llvm::LoopInfo &amp; LI, llvm::ScalarEvolution * SE, llvm::IRBuilderBase &amp; Builder, SmallVectorImpl&lt;llvm::PHINode *&gt; * PHIsToRemove = nullptr)'/>
<use f='llvm/llvm/lib/Transforms/Utils/LCSSA.cpp' l='390' u='c' c='_ZN4llvm9formLCSSAERNS_4LoopERKNS_13DominatorTreeEPKNS_8LoopInfoEPNS_15ScalarEvolutionE'/>
<doc f='llvm/llvm/lib/Transforms/Utils/LCSSA.cpp' l='76'>/// For every instruction from the worklist, check to see if it has any uses
/// that are outside the current loop.  If so, insert LCSSA PHI nodes and
/// rewrite the uses.</doc>
<use f='llvm/llvm/lib/Transforms/Utils/ScalarEvolutionExpander.cpp' l='2595' u='c' c='_ZN4llvm12SCEVExpander17fixupLCSSAFormForEPNS_11InstructionEj'/>
