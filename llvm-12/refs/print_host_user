<dec f='llvm/polly/lib/External/ppcg/cuda.h' l='10' type='isl_printer * print_host_user(isl_printer * p, isl_ast_print_options * print_options, isl_ast_node * node, void * user)'/>
<def f='llvm/polly/lib/External/ppcg/cuda.c' l='598' ll='662' type='isl_printer * print_host_user(isl_printer * p, isl_ast_print_options * print_options, isl_ast_node * node, void * user)'/>
<use f='llvm/polly/lib/External/ppcg/cuda.c' l='674' u='a' c='print_host_code'/>
<doc f='llvm/polly/lib/External/ppcg/cuda.c' l='585'>/* Print the user statement of the host code to &quot;p&quot;.
 *
 * The host code may contain original user statements, kernel launches,
 * statements that copy data to/from the device and statements
 * the initialize or clear the device.
 * The original user statements and the kernel launches have
 * an associated annotation, while the other statements do not.
 * The latter are handled by print_device_node.
 * The annotation on the user statements is called &quot;user&quot;.
 *
 * In case of a kernel launch, print a block of statements that
 * defines the grid and the block and then launches the kernel.
 */</doc>
