<def f='llvm/clang/include/clang/AST/PrettyPrinter.h' l='51' type='bool clang::PrintingCallbacks::isScopeVisible(const clang::DeclContext * DC) const'/>
<doc f='llvm/clang/include/clang/AST/PrettyPrinter.h' l='44'>/// When printing type to be inserted into code in specific context, this
  /// callback can be used to avoid printing the redundant part of the
  /// qualifier. For example, when inserting code inside namespace foo, we
  /// should print bar::SomeType instead of foo::bar::SomeType.
  /// To do this, shouldPrintScope should return true on &quot;foo&quot; NamespaceDecl.
  /// The printing stops at the first isScopeVisible() == true, so there will
  /// be no calls with outer scopes.</doc>
<use f='llvm/clang/lib/AST/TypePrinter.cpp' l='1228' u='c' c='_ZN12_GLOBAL__N_111TypePrinter11AppendScopeEPN5clang11DeclContextERN4llvm11raw_ostreamENS1_15DeclarationNameE'/>
