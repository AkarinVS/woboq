<dec f='llvm/clang/include/clang/StaticAnalyzer/Core/PathSensitive/ProgramStateTrait.h' l='132'/>
<doc f='llvm/clang/include/clang/StaticAnalyzer/Core/PathSensitive/ProgramStateTrait.h' l='121'>/// Declares an immutable list type \p NameTy, suitable for placement into
  /// the ProgramState. This is implementing using llvm::ImmutableList.
  ///
  /// \code
  /// State = State-&gt;add&lt;Name&gt;(E); // Adds to the /end/ of the list.
  /// bool Present = State-&gt;contains&lt;Name&gt;(E);
  /// NameTy List = State-&gt;get&lt;Name&gt;();
  /// \endcode
  ///
  /// The macro should not be used inside namespaces, or for traits that must
  /// be accessible from more than one translation unit.</doc>
<use f='llvm/clang/lib/StaticAnalyzer/Checkers/PthreadLockChecker.cpp' l='250' u='c'/>
<use f='llvm/clang/lib/StaticAnalyzer/Core/LoopUnrolling.cpp' l='66' u='c'/>
