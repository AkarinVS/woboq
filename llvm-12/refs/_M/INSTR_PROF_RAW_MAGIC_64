<dec f='llvm/llvm/include/llvm/ProfileData/InstrProfData.inc' l='640'/>
<use f='llvm/llvm/include/llvm/ProfileData/InstrProf.h' l='1108' u='c'/>
<doc f='llvm/llvm/include/llvm/ProfileData/InstrProfData.inc' l='633'>/* Magic number to detect file format and endianness.
 * Use 255 at one end, since no UTF-8 file can use that character.  Avoid 0,
 * so that utilities, like strings, don&apos;t grab it as a string.  129 is also
 * invalid UTF-8, and high enough to be interesting.
 * Use &quot;lprofr&quot; in the centre to stand for &quot;LLVM Profile Raw&quot;, or &quot;lprofR&quot;
 * for 32-bit platforms.
 */</doc>
<dec f='llvm/compiler-rt/include/profile/InstrProfData.inc' l='640'/>
<doc f='llvm/compiler-rt/include/profile/InstrProfData.inc' l='633'>/* Magic number to detect file format and endianness.
 * Use 255 at one end, since no UTF-8 file can use that character.  Avoid 0,
 * so that utilities, like strings, don&apos;t grab it as a string.  129 is also
 * invalid UTF-8, and high enough to be interesting.
 * Use &quot;lprofr&quot; in the centre to stand for &quot;LLVM Profile Raw&quot;, or &quot;lprofR&quot;
 * for 32-bit platforms.
 */</doc>
<use f='llvm/compiler-rt/lib/profile/InstrProfiling.c' l='24' u='c'/>
