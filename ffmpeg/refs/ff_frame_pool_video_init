<dec f='ffmpeg/libavfilter/framepool.h' l='47' type='FFFramePool * ff_frame_pool_video_init(AVBufferRef *(*)(buffer_size_t) alloc, int width, int height, enum AVPixelFormat format, int align)'/>
<doc f='ffmpeg/libavfilter/framepool.h' l='35'>/**
 * Allocate and initialize a video frame pool.
 *
 * @param alloc a function that will be used to allocate new frame buffers when
 * the pool is empty. May be NULL, then the default allocator will be used
 * (av_buffer_alloc()).
 * @param width width of each frame in this pool
 * @param height height of each frame in this pool
 * @param format format of each frame in this pool
 * @param align buffers alignement of each frame in this pool
 * @return newly created video frame pool on success, NULL on error.
 */</doc>
<def f='ffmpeg/libavfilter/framepool.c' l='51' ll='117' type='FFFramePool * ff_frame_pool_video_init(AVBufferRef *(*)(buffer_size_t) alloc, int width, int height, enum AVPixelFormat format, int align)'/>
<use f='ffmpeg/libavfilter/video.c' l='68' u='c' c='ff_default_get_video_buffer'/>
<use f='ffmpeg/libavfilter/video.c' l='83' u='c' c='ff_default_get_video_buffer'/>
