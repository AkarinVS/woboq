<def f='ffmpeg/libavcodec/webp.c' l='148' ll='168'/>
<use f='ffmpeg/libavcodec/webp.c' l='178'/>
<use f='ffmpeg/libavcodec/webp.c' l='416' c='decode_entropy_coded_image'/>
<use f='ffmpeg/libavcodec/webp.c' l='549' c='decode_entropy_coded_image'/>
<doc f='ffmpeg/libavcodec/webp.c' l='142'>/* The structure of WebP lossless is an optional series of transformation data,
 * followed by the primary image. The primary image also optionally contains
 * an entropy group mapping if there are multiple entropy groups. There is a
 * basic image type called an &quot;entropy coded image&quot; that is used for all of
 * these. The type of each entropy coded image is referred to by the
 * specification as its role. */</doc>
