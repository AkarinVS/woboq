<dec f='ffmpeg/libavfilter/avfilter.h' l='943' type='AVFilterContext * avfilter_graph_alloc_filter(AVFilterGraph * graph, const AVFilter * filter, const char * name)'/>
<doc f='ffmpeg/libavfilter/avfilter.h' l='929'>/**
 * Create a new filter instance in a filter graph.
 *
 * @param graph graph in which the new filter will be used
 * @param filter the filter to create an instance of
 * @param name Name to give to the new instance (will be copied to
 *             AVFilterContext.name). This may be used by the caller to identify
 *             different filters, libavfilter itself assigns no semantics to
 *             this parameter. May be NULL.
 *
 * @return the context of the newly created filter instance (note that it is
 *         also retrievable directly through AVFilterGraph.filters or with
 *         avfilter_graph_get_filter()) on success or NULL on failure.
 */</doc>
<use f='ffmpeg/fftools/ffmpeg_filter.c' l='381' u='c' c='insert_trim'/>
<use f='ffmpeg/libavfilter/avfiltergraph.c' l='148' u='c' c='avfilter_graph_create_filter'/>
<def f='ffmpeg/libavfilter/avfiltergraph.c' l='170' ll='204' type='AVFilterContext * avfilter_graph_alloc_filter(AVFilterGraph * graph, const AVFilter * filter, const char * name)'/>
<use f='ffmpeg/libavfilter/graphparser.c' l='132' u='c' c='create_filter'/>
