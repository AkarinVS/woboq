<dec f='halide/llvm-install/include/llvm/IR/Instructions.h' l='133' type='bool llvm::AllocaInst::isStaticAlloca() const'/>
<doc f='halide/llvm-install/include/llvm/IR/Instructions.h' l='130'>/// Return true if this alloca is in the entry block of the function and is a
  /// constant size. If so, the code generator will fold it into the
  /// prolog/epilog code, so it is basically free.</doc>
