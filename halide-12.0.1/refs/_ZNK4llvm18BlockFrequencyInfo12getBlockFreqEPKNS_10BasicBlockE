<dec f='halide/llvm-install/include/llvm/Analysis/BlockFrequencyInfo.h' l='65' type='llvm::BlockFrequency llvm::BlockFrequencyInfo::getBlockFreq(const llvm::BasicBlock * BB) const'/>
<doc f='halide/llvm-install/include/llvm/Analysis/BlockFrequencyInfo.h' l='60'>/// getblockFreq - Return block frequency. Return 0 if we don&apos;t have the
  /// information. Please note that initial frequency is equal to ENTRY_FREQ. It
  /// means that we should not rely on the value itself, but only on the
  /// comparison to the other block frequencies. We do this to avoid using of
  /// floating points.</doc>
