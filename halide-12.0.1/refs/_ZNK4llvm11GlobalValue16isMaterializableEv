<dec f='halide/llvm-install/include/llvm/IR/GlobalValue.h' l='523' type='bool llvm::GlobalValue::isMaterializable() const'/>
<doc f='halide/llvm-install/include/llvm/IR/GlobalValue.h' l='513'>/// @name Materialization
  /// Materialization is used to construct functions only as they&apos;re needed.
  /// This
  /// is useful to reduce memory usage in LLVM or parsing work done by the
  /// BitcodeReader to load the Module.
  /// @{

  /// If this function&apos;s Module is being lazily streamed in functions from disk
  /// or some other source, this method can be used to check to see if the
  /// function has been read in yet or not.</doc>
