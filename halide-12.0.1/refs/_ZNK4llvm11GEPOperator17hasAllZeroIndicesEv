<def f='halide/llvm-install/include/llvm/IR/Operator.h' l='521' ll='529' type='bool llvm::GEPOperator::hasAllZeroIndices() const'/>
<doc f='halide/llvm-install/include/llvm/IR/Operator.h' l='518'>/// Return true if all of the indices of this GEP are zeros.
  /// If so, the result pointer and the first operand have the same
  /// value, just potentially different types.</doc>
