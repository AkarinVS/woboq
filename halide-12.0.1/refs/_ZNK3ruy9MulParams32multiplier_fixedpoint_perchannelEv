<def f='halide/build-apps/ruy/ruy/mul_params.h' l='133' ll='136' type='const ruy::MulParams::AccumScalar * ruy::MulParams::multiplier_fixedpoint_perchannel() const'/>
<doc f='halide/build-apps/ruy/ruy/mul_params.h' l='125'>// Per-channel variant of multiplier_fixedpoint. Setting this switches
  // to per-channel mode, where `multiplier_fixedpoint` and
  // `multiplier_exponent` are disabled and `multiplier_fixedpoint_perchannel`
  // and `multiplier_exponent_perchannel` are used instead.
  //
  // This must point to a buffer of as many values as there are rows in the
  // destination matrix. Each row of the destination matrix will use the
  // corresponding buffer element instead of multiplier_fixedpoint.</doc>
