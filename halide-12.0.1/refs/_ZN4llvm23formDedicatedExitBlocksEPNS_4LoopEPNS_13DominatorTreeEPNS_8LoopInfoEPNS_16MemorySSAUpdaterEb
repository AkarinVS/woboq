<dec f='halide/llvm-install/include/llvm/Transforms/Utils/LoopUtils.h' l='64' type='bool llvm::formDedicatedExitBlocks(llvm::Loop * L, llvm::DominatorTree * DT, llvm::LoopInfo * LI, llvm::MemorySSAUpdater * MSSAU, bool PreserveLCSSA)'/>
<doc f='halide/llvm-install/include/llvm/Transforms/Utils/LoopUtils.h' l='59'>/// Ensure that all exit blocks of the loop are dedicated exits.
///
/// For any loop exit block with non-loop predecessors, we split the loop
/// predecessors to use a dedicated loop exit block. We update the dominator
/// tree and loop info if provided, and will preserve LCSSA if requested.</doc>
