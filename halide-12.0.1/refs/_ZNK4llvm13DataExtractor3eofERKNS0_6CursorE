<def f='halide/llvm-install/include/llvm/Support/DataExtractor.h' l='655' type='bool llvm::DataExtractor::eof(const llvm::DataExtractor::Cursor &amp; C) const'/>
<doc f='halide/llvm-install/include/llvm/Support/DataExtractor.h' l='651'>/// Return true iff the cursor is at the end of the buffer, regardless of the
  /// error state of the cursor. The only way both eof and error states can be
  /// true is if one attempts a read while the cursor is at the very end of the
  /// data buffer.</doc>
